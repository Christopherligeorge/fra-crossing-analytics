name: crossing_analytics
version: '1.0.0'
config-version: 2
profile: crossing_analytics
model-paths: [models]
test-paths: [tests]
macro-paths: [macros]
clean-targets: [target, dbt_packages]
vars:
  first_year: 2014
  last_year: 2025
  full_release: false
models:
  crossing_analytics:
    +persist_docs: {relation: true, columns: true}
    staging:
      +materialized: table
    intermediate:
      +materialized: table
    marts:
      +materialized: table

