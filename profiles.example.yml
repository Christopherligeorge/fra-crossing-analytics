# Safe template; scripts/build.py supplies absolute paths, no credentials required.
crossing_analytics:
  target: local
  outputs:
    local:
      type: duckdb
      path: "{{ env_var('CROSSING_DB') }}"
      threads: 2
      settings:
        memory_limit: 3GB
        preserve_insertion_order: false

